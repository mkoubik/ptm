<div class="task">
  <h2><%= task.title %></h2>
  <p><%= task.comment %></p>
  <p>
    <% if task.finished? %>
      Finished: <%= task.finished %>
    <% else %>
      <%= link_to "Finish", finish_task_url(task) %>
    <% end %>
    | <%= link_to "Delete", delete_task_url(task) %>
  </p>
</div>
